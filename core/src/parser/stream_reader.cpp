#include "stream_reader.h"

StreamReader::StreamReader(int bufferSize): bufferSize(bufferSize) {}

char StreamReader::peek(int k) {
  return 'a';
}

char StreamReader::consume(int k) {
  return 'a';
}